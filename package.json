{
  "name": "yuno-dev",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.12.1",
  "scripts": {
    "compile": "pnpm --filter teul compile",
    "build:yuno.dev": "pnpm compile && pnpm -F yuno.dev build:cloudflare"
  },
  "devDependencies": {
    "@types/node": "^24.3.1",
    "typescript": "^5.9.2"
  }
}
